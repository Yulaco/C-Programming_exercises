#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Successful)
 */
int main(void)
{
	int m = -5;

	if (m > 0)
		printf("The value of n = 1\n");
	if (m == 0)
		printf("The value of n = 0\n");
	else
		printf("The value of n = -1\n");
	return (0);
}
