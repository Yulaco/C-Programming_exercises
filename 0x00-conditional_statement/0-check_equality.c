#include <stdio.h>

/**
 * main - Entry point.
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
	int num1 = 15;
	int num2 = 15;

	if (num1 == num2)
		printf("The two numbers are equal\n");

	else
		printf("The two numbers are not equal\n");

	return (0);
}
